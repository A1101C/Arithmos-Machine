---
// src/components/linemath.astro
import katex from 'katex';

interface Props {
  formula: string;
}

const { formula } = Astro.props;

//displayMode is false so it stays inside the paragraph text
const html = katex.renderToString(formula, { displayMode: false });
---
<span set:html={html} />